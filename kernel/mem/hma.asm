; ===========================================================================
; claudeDOS High Memory Area (HMA) Support - Stub (Phase 9)
; ===========================================================================

hma_available   db  0           ; 1 = HMA is available
dos_in_hma      db  0           ; 1 = DOS loaded high

; ---------------------------------------------------------------------------
; hma_enable_a20 - Enable A20 gate (Phase 9)
; ---------------------------------------------------------------------------
hma_enable_a20:
    ret

; ---------------------------------------------------------------------------
; hma_disable_a20 - Disable A20 gate (Phase 9)
; ---------------------------------------------------------------------------
hma_disable_a20:
    ret
